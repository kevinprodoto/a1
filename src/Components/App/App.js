import React from "react";

import AppHeader from "../AppHeader";

import ItemsList from "../ItemsList"

export const App = () => {
    return (
        <section>
            <AppHeader />
            <ItemsList />
        </section>
    )
}